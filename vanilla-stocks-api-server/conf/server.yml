server:
  port: 1987
  servlet:
    context-path: /api/v1

spring:
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

log:
  path:
  file-name: vanilla-api
  file-extention: log
  level: debug
  max-history: 30
  max-size: 300MB
  show-sql: true
  